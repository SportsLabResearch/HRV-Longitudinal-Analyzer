# Generador EXE optimizado

Usar únicamente:

`GENERAR_EXE_WINDOWS.bat`

Esta versión no instala todas las dependencias del archivo requirements.txt.
Comprueba el ordenador local e instala solo lo que falta para este proyecto.

También evita que PyInstaller incluya librerías pesadas instaladas en tu Python pero no usadas por HRV-Longitudinal-Analyzer.
